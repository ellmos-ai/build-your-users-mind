# Alternate banner ideas

Reserve banner concepts (not the active one — that is `assets/banner.svg`).

- `banner-scan.svg` — animated: a human figure is scanned by a moving line and dissolves into
  rising letters/glyphs (the human translated into language). Self-contained, CSS-animated SVG.

To use one: copy it over `assets/banner.svg`, or change the `<img>` path in `README.md`.
